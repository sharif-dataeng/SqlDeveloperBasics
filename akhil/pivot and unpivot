SELECT SalesPerson, [2023] AS Sales2023, [2024] AS Sales2024
FROM (
    SELECT SalesPerson, [Year], SalesAmount
    FROM Sales1
) AS SourceTable
PIVOT (
    SUM(SalesAmount)
    FOR [Year] IN ([2023], [2024])
) AS PivotTable;